# M9 — ESP32 Rust Firmware Foundation

**Status: IN PROGRESS.** Board-free work substantially complete; hardware
verification **pending**.
**Board-free pass: 2026-09-02.** No physical ESP32-C3 was available.

> **Nothing in this report is evidence about silicon.** Every criterion below is
> labelled with what was actually done to it: **VERIFIED (no hardware)** means a
> test ran and passed on a host; **IMPLEMENTED, HARDWARE VERIFICATION PENDING**
> means the code exists and compiles for the target but no board has executed
> it; **NOT STARTED** means what it says. A compile, a host mock, a simulator
> and an argument are none of them a board, and the distinction is kept
> throughout because M9's one gate that genuinely matters — HIL-1 — cannot be
> approximated.

---

## 1. What M9-001 actually found

M9-001's job was to execute ADR-007's toolchain section on a real machine and
correct it from what happened. It happened, and the ADR was wrong in four
places. [ADR-007](../adr/007-esp32-rust-framework-and-toolchain.md) now records
the corrections; this is the summary.

### The Rust-version question is resolved: outcome 2

**Stock Rust 1.98.0 cannot build `riscv32imc-esp-espidf`.**

```text
$ rustup target add riscv32imc-esp-espidf --toolchain 1.98.0-x86_64-pc-windows-msvc
error: toolchain '1.98.0-x86_64-pc-windows-msvc' has no prebuilt artifacts
       available for target 'riscv32imc-esp-espidf'
```

`rustc --print target-list` knows the triple — it is a recognised tier-3 target
— but no `std` is distributed for it, so `std` must be built from source with
`-Z build-std=std,panic_abort`, which is nightly-only. ADR-007 anticipated this
as one of two acceptable outcomes and it is the one that happened.

The firmware **image** workspace therefore pins `nightly-2026-07-01`
(rustc 1.98.0-nightly, f46ec5218) with `rust-src`. A dated nightly, not
floating: CI runs clippy with `-D warnings`, and a floating nightly turns a
green branch red with no code change.

**The exception is contained more tightly than ADR-007 required.** The host
workspace is untouched at 1.98.0, and so is `firmware/node-app` — where the
actuation gate's caller, the ledger, the budget, the policy store, and their 139
tests live. Only the crate that links ESP-IDF needs nightly.

### espup does not do what the ADR said it did

ADR-007's stated reason for using espup on a RISC-V target was that "it also
provisions the ESP-IDF C environment that `esp-idf-sys` needs, which is the
harder half". It does not. `esp-idf-sys`'s build script clones ESP-IDF itself
and installs its own toolchain, cmake, ninja, and Python virtualenv into
`<drive>/.embuild/espressif`.

### `espup install --targets esp32c3` is not sufficient, and fails late

For a RISC-V-only selection espup adds three bare-metal `riscv32*-none-elf`
targets, writes an **empty** export script, and installs no LLVM. The build then
runs all the way through cloning ESP-IDF and compiling its ~110 C components —
341 seconds — before failing:

```text
Unable to find libclang: "couldn't find any valid shared libraries matching:
['clang.dll', 'libclang.dll'], set the `LIBCLANG_PATH` environment variable"
```

The `libclang` `esp-idf-sys`'s bindgen needs ships with the LLVM espup installs
alongside the **Xtensa** toolchain. `espup install --targets all --std` is
required, even though nothing here builds for Xtensa.

### Both halves of the export script are load-bearing on Windows

It sets `LIBCLANG_PATH` to the DLL *and* prepends `esp-clang/bin` to `PATH`.
Setting only `LIBCLANG_PATH` fails with `LoadLibraryExW failed` — the DLL is
found and cannot be loaded, because its dependencies live in that directory.
Measured both ways: with the directory on `PATH` the build succeeds in 29.5 s;
without it, it fails.

### The Windows path limit is a hard stop

`esp-idf-sys` refuses an `OUT_DIR` longer than **88 characters** after
canonicalisation. The fixed suffix cargo appends is 73 characters, so
`CARGO_TARGET_DIR` must be **15 characters or fewer**. This repository's natural
target directory is 47.

```powershell
$env:CARGO_TARGET_DIR = "D:\rzt"
```

`ESP_IDF_PATH_ISSUES=warn` downgrades it to a warning and is recorded only so
nobody has to find it; it moves where the build fails rather than fixing
anything.

### Versions that build

| Component | Version | ADR-007 had guessed |
|---|---|---|
| Firmware toolchain | `nightly-2026-07-01` | 1.98.0 or "espup channel" |
| Host toolchain (unchanged) | 1.98.0 | 1.98.0 |
| Target | `riscv32imc-esp-espidf` | same |
| `esp-idf-svc` | 0.52.1 | 0.52 |
| `esp-idf-hal` | 0.46.2 | 0.45 |
| `esp-idf-sys` | 0.37.2 | 0.36 |
| `embedded-svc` | 0.29.0 | 0.28 |
| `embuild` | 0.33.4 | — |
| ESP-IDF | v5.5.1 | — |
| `bindgen` (transitive) | 0.71.1 | — |
| `espup` / `ldproxy` / `espflash` | 0.17.1 / 0.3.5 / 4.5.0 | — |
| Python (host) | 3.14.2, accepted by IDF 5.5.1 | — |

All pinned with `=` in `firmware/esp32-node/Cargo.toml`.

### Build times, measured

| | |
|---|---|
| ESP-IDF clone, tool install, and C build (cold) | **341 s**, ~5.4 GB in `.embuild` |
| Rust compile after that | ~30 s |
| Warm rebuild after a source edit | **~2.5 s** |
| Rebuild after `cargo clean -p esp-idf-sys` | ~30 s |

A genuinely cold build is about **six and a half minutes** plus downloads. Every
build after that is seconds, and repeated builds need no network — ESP-IDF is
cloned once to a pinned tag and the `=`-pinned crates resolve from the local
registry.

### The environment and the reproducible commands

Windows 11 Pro 10.0.22631, native (not WSL2, not a container).

```powershell
cargo install espup ldproxy
espup install --targets all --std     # NOT --targets esp32c3
. $HOME\export-esp.ps1                # sets LIBCLANG_PATH *and* PATH
$env:CARGO_TARGET_DIR = "D:\rzt-fw"   # 15 chars or fewer

cd firmware/esp32-node
cargo build --release                 # ~6.5 min cold, ~2.5 s warm
```

Output: `D:\rzt-fw\riscv32imc-esp-espidf\release\esp32-node`, ELF 32-bit LSB,
UCB RISC-V, RVC, soft-float ABI, statically linked — **3.94 MB** with the full
session loop.

---

## 2. What was built

Two workspaces, and the split is a deliberate improvement on ADR-007's sketch.

```text
firmware/
├── node-app/       WORKSPACE A — host toolchain 1.98.0, no ESP-IDF dependency
└── esp32-node/     WORKSPACE B — nightly + build-std, riscv32imc-esp-espidf
```

ADR-007 put the host-testable layer in `firmware/esp32-node/src/app/` and
enforced "no `esp_idf_*` import" with a grep. A separate crate makes the same
property **structural** — the crate has no ESP-IDF dependency, so an ESP-IDF
symbol there does not compile — and ADR-007 explicitly permitted a cleaner
separation that achieves the same isolation. Two consequences, both wanted: the
safety logic is compiled by the host pin rather than by nightly, and
`cargo test` in `node-app` needs no target flag, no `build-std`, and no ESP-IDF
installation at all.

| Concern | Module |
|---|---|
| hardware traits and their fakes | `ports.rs`, `fakes.rs` |
| boot ordering (SAFETY-011) | `boot.rs` |
| persisted state, CRC | `persist.rs` |
| identity, boot identity | `identity.rs` |
| **the one actuation path** | `command.rs` |
| pending-result ledger | `ledger.rs` |
| dedup ring (SAFETY-001) | `dedup.rs` |
| interrupted dose (SAFETY-011) | `recovery.rs` |
| configuration (§5.7) | `config.rs` |
| policy store, atomic activation (SAFETY-019) | `policy.rs` |
| **the one offline evaluator call site** | `offline.rs` |
| monotonic budget, `credit_elapsed` (SAFETY-015) | `budget.rs` |
| event buffer, gaps (SAFETY-016, -020) | `buffer.rs` |
| power mode, wake cycle (ADR-018) | `power.rs`, `awake_hold.rs` |
| rails, warm-up | `sampling.rs` |
| telemetry, ring, status | `telemetry.rs` |
| serial provisioning | `provision.rs` |

The ESP crate holds `main.rs` (pump-off first), `run.rs` (the session loop),
`board/` (the only place a GPIO number appears), `hal/` (ESP-IDF adapters), and
`net/`.

---

## 3. Board-free criteria: VERIFIED (no hardware)

Every line here corresponds to a test that ran and passed, or a command that was
executed, on 2026-09-02.

### Build and toolchain

- [x] `cargo build --release` succeeds for `riscv32imc-esp-espidf` with no board.
- [x] The contract crate and `rhizo-policy` are **path** dependencies with
      `default-features = false`, asserted by
      `board_isolation::the_shared_crates_are_path_dependencies_with_default_features_off`.
- [x] ADR-007's toolchain section has been executed and corrected (§1).
- [x] ADR-007's firmware-structure section matches the layout as built.
- [x] Exact crate versions pinned; the firmware Rust version resolved and pinned.
- [x] The host workspace toolchain is unchanged at 1.98.0.
- [x] Cold build time measured (§1).
- [x] Offline and repeated builds work after setup.

### Board isolation (M9-003, F-090-43…48)

- [x] `board-devkitm1` exists, is the default profile, and builds.
- [x] **Zero** board features fails with a `compile_error!` naming the available
      profiles. Executed:

      ```text
      error: no board profile is selected. Available board profiles:
      `board-devkitm1` (Espressif ESP32-C3-DEVKITM-1-N4X, the reference board),
      `board-xiao-esp32c3` (Seeed XIAO ESP32-C3, reserved). …
      ```

- [x] **Two** board features fails the same way:

      ```text
      error: exactly one board profile must be selected, and two are:
      `board-devkitm1` and `board-xiao-esp32c3`. …
      ```

- [x] No concrete GPIO number or pin polarity outside `src/board/`, asserted by
      `board_isolation::no_gpio_number_or_polarity_outside_the_board_layer`.
- [x] The board layer *does* name pins — the negative check would also pass if
      nobody had written a pin map, so there is a positive one too.
- [x] `deep_sleep` has exactly one call site, in `hal/sleep.rs`.
- [x] No stabilisation constant for any specific sensor part appears anywhere.
- [x] No battery field appears in the irrigation path.
- [x] The `app` tests are board-independent by construction: `node-app` does not
      depend on `esp32-node`, so no board profile is involved in running them.

### The single actuation path

- [x] Exactly **one** `validate_water_command` call site, in `command.rs`.
- [x] Exactly **one** `evaluate_offline` call site, in `offline.rs`.
- [x] Exactly **one** function drives the pump (`command::execute_authorised`),
      so the autonomous and commanded paths cannot diverge on the in-flight NVS
      write or the hard limits.
- [x] No catch-all `_ =>` arm in `command.rs`, `offline.rs`, `budget.rs`, or
      `policy.rs` (SAFETY-012 as a source rule).

### Safety behaviour, with fake adapters

- [x] **SAFETY-011** — `pump_off()` is the first statement in `main`, asserted
      by recording the call sequence: `["pump_off", "rail_disable(sensor)",
      "rail_disable(rs485)"]`. No initialisation path runs the pump.
- [x] **SAFETY-011** — an unfinished NVS dose on boot produces
      `status: "interrupted"` with `delivered_ml` **null**, never `0.0`; the
      dose is not resumed; the record survives a failure to publish.
- [x] **SAFETY-011** — a failed NVS write **aborts** the dose: nothing pumped,
      result `failed`, daily total unchanged.
- [x] **SAFETY-001** — a duplicate `command_id` republishes the stored result
      and does not actuate, **across a simulated power cycle** (the ring is
      reloaded from the fake NVS).
- [x] **SAFETY-002** — every water command refused with `clock_unsynced` while
      unsynchronised.
- [x] **SAFETY-007** — an oversized command clamps to `FIRMWARE_MAX_ML_PER_RUN`;
      the device daily cap is enforced independently of the edge; no
      configuration field can change a reported limit.
- [x] **SAFETY-012** — an unreadable leak sensor is `Unknown` and `Unknown` is a
      refusal, on both the commanded and the autonomous path.
- [x] **SAFETY-013** — an isolated device with no policy never waters, and the
      refusal is buffered; a corrupt policy store refuses and substitutes **no**
      default.
- [x] **SAFETY-015** — `credit_elapsed` credits measured time only for a timer
      wake with a valid RTC checksum; a cold boot, an external wake, a watchdog
      reset, a failed checksum, an absent RTC block, and a backwards counter all
      credit **zero**. A reboot loop never completes a confirmation and never
      replenishes the budget.
- [x] **SAFETY-016** — `event_id` is stable across repeated replays.
- [x] **SAFETY-019** — interruption at **every** step of a policy update leaves
      exactly one valid active policy; a lower or equal version is ignored; an
      invalid policy leaves the previous one active.
- [x] **SAFETY-020** — audit events survive a telemetry flood; eviction records
      a gap with the correct range and count; a sealed gap is immutable and a
      later loss opens a new one; a sealed marker outranks everything buffered
      before it; an unsealed gap is never replayed.
- [x] **SAFETY-021** — an absent or unrecognised `power.mode` yields
      `AlwaysOn`; sleep is refused while a hold is outstanding and before the
      announcement is acknowledged.
- [x] An `event.ack` beyond the highest allocated sequence is refused **whole,
      not clamped**; one naming another boot is ignored entirely.

### The pending-result ledger (M9-011) — decided, documented, tested

The decision is in §5. Verified with fake adapters:

- [x] Actuation is refused one slot before the ledger is full, with
      `RejectReason::ResultLedgerFull`.
- [x] The reserved slot holds the refusal the saturation itself causes.
- [x] A full ledger **never** discards an unacknowledged result — a refused
      insert leaves every entry and every millilitre in place.
- [x] Acknowledgement frees exactly one slot, restores actuation, and clears the
      fault; the fault latches once per episode, not once per command.
- [x] An acknowledgement for an entry not held is a no-op and clears nothing.
- [x] A republished result updates in place and takes no second slot.
- [x] The state at saturation survives a power cycle with the fault still
      raised, so it is not re-emitted on every boot.

### Conformance (M9-014) — 12 tests, all passing

The same command driven against the reference simulator and against the firmware
with fakes, comparing the `command.result` **decision** — status, reason,
clamped, delivered volume, running total, origin — with the **sensor readings
taken from the simulator** so the comparison isolates the decision from the
model that produced its inputs.

- [x] accepted dose · oversized clamp · `clock_unsynced` · `expired` ·
      `malformed_command` · `leak_detected` · `tank_low` · duplicate ·
      `over_daily_max` (reached on the seventh 80 ml dose in both).
- [x] The subscription sets are identical, and neither subscribes to
      `commands/result`.
- [x] All twenty offline refusal-reason names are identical between the two
      independent `match` statements.
- [x] **A deliberate divergence fails the test** — the negative check feeds the
      firmware a clear leak sensor where the simulator sees water and asserts
      the comparison notices.

**The conformance suite earned its place immediately.** It found a real
divergence: the firmware published `delivered_ml: Some(0.0)` on a rejection
where the simulator publishes `null`. Both decode, both read plausibly, and no
type would ever have caught it. Protocol §5.10's table says a rejection credits
no volume — there is nothing to report, not a measured zero — so the simulator
is canonical and the firmware was changed to match. This is exactly the class of
drift M9-014 exists for.

### Host gates

```text
firmware/node-app:  cargo fmt --all --check          clean
                    cargo clippy --all-targets --all-features -- -D warnings   clean
                    cargo test --all-features        139 passed, 0 failed
                      lib                            111
                      conformance                     12
                      board_isolation                  8
                      single_actuation_path            5
                      fixtures                         3

firmware/esp32-node: cargo fmt --all --check         clean
                    cargo clippy --release --all-targets -- -D warnings        clean
                    cargo build --release            ok

root workspace:     cargo fmt --all --check          clean
                    cargo clippy --workspace --all-targets --all-features -- -D warnings   clean
                    cargo test --workspace --all-features   1171 passed, 0 failed, 1 ignored
                    cargo build -p rhizo-mqtt-contract --no-default-features
                      --target thumbv7em-none-eabi   ok
                    cargo build -p rhizo-policy --no-default-features
                      --target thumbv7em-none-eabi   ok
                    cargo run -p rhizo-docscheck     OK, 7454 checks
                    docker compose config            ok (via WSL2)

broker-backed, RHIZO_REQUIRE_BROKER=1, Mosquitto up:
                    device-simulator --test integration      19 passed
                    device-simulator --test safety_007        2 passed
                    edge-controller  --test integration      20 passed

M8 regression:      ./scripts/run-scenarios.sh       41 scenarios PASSED, 0 failed
                                                     (script exit 0)

GitHub Actions, run 33690316017 (the first CI run carrying this work):
                    docs                             pass
                    fmt / clippy / test              pass
                    integration (broker)             pass
                    compose config                   pass
                    firmware app (host)              pass, 1m42s
                    firmware image (board-devkitm1)  pass, 8m50s cold
```

> **The image builds on Linux as well as Windows, and that is now measured
> rather than assumed.** ADR-007 carried a Linux container as the documented
> fallback for Windows friction; the CI job is that path, and it ran the whole
> sequence — `espup install --targets all --std`, the ESP-IDF bootstrap, the
> `-Z build-std` cross build, clippy at `-D warnings`, and both `compile_error!`
> guards — green in 8m50s from cold. Warm-cache timing is not yet known; M9-002
> asks for warm builds under five minutes and one cold run cannot answer that.

> **The workspace total is quoted with its environment, deliberately.** As
> CLAUDE.md records, 1171 is identical with the broker stopped and with it
> running — 46 broker-gated tests skip silently and count as passed — so the
> number alone proves nothing about broker behaviour. **The three broker-backed
> suites were therefore run separately with `RHIZO_REQUIRE_BROKER=1` against a
> live Mosquitto**, where a missing broker is a failure rather than a skip:
> 41 tests, all passing. That is the evidence, not the total.
>
> The **M8 scenario suite was re-run in full** because this pass changed the
> shared contract crate (`RejectReason::ResultLedgerFull`). All 41 scenarios
> pass on the complete Docker topology — including the sleeping-device and
> battery scenarios, which are the ones a `RejectReason` change could plausibly
> have disturbed.
>
> **Read the script's own exit status, not a scenario tally scraped from the
> log.** `run-scenarios.sh | tail -n` returns `tail`'s exit code, so a piped run
> reports success while a scenario has failed, and a `grep -c PASSED` over the
> truncated tail undercounts. An earlier note in this report said "14 scenarios"
> for exactly that reason; the suite has 41. The reliable form is
> `./scripts/run-scenarios.sh > log 2>&1; echo $?`.

---

## 4. IMPLEMENTED, HARDWARE VERIFICATION PENDING

Code exists, compiles for `riscv32imc-esp-espidf`, and is reviewed. **No board
has executed any of it.** Nothing below may be treated as verified.

| Criterion | Where | Why only a board settles it |
|---|---|---|
| Wi-Fi association, reconnect with full-jitter backoff, RSSI | `net/wifi.rs` | needs a radio and a router that misbehaves |
| MQTT connect, **LWT set before connect**, `clean_session`, eight exact subscriptions | `net/mqtt.rs`, `net/session.rs` | the LWT is only observable by killing power |
| Retained `status: online` on connect; heartbeat every 5 intervals | `run.rs` | needs a broker and an edge |
| `edge.time` applied, `clock_synced` age-out, unsynced status republish ≤ 60 s | `run.rs`, `hal/clock.rs` | the *rules* are shared-crate code and host-tested; the wiring is not |
| Telemetry publish and the 16-sample ring across a disconnect | `run.rs` | the ring is host-tested; publishing is not |
| `command.result` republished every 15 s until `command.result.ack` | `run.rs` | the ledger is host-tested; the timer on silicon is not |
| History replay, gap sealing before each replay | `run.rs` | as above |
| Real GPIO pump drive and de-energise | `hal/pump.rs`, `board/devkitm1.rs` | **HIL-1** |
| Power rails on real load switches | `hal/rail.rs` | a meter |
| NVS on real flash, two CRC slots, A/B pointer flip | `hal/nvs.rs` | real flash, real power cuts |
| RTC-retained state in `.rtc.data` across reset causes | `hal/rtc_store.rs` | only silicon has an RTC domain |
| `esp_deep_sleep` and timer wake | `hal/sleep.rs` | as above |
| Task watchdog subscription and reset behaviour | `hal/watchdog.rs` | a real watchdog reset |
| MAC-derived identity | `main.rs` | needs a real MAC |
| Serial provisioning over the USB-UART bridge | `provision.rs` (parser host-tested) | the console itself needs a cable |
| Battery divider (`DividerBattery`) | `hal/battery.rs` | the reference board has no divider; M10-012 |

### The exact criteria that remain hardware-blocked

Verbatim from PRD 090 and M9-022, none of them started:

- [ ] **With a board:** it connects, appears online in the edge API, publishes
      telemetry from fake sensors, applies retained config, and echoes
      `applied_config_version`.
- [ ] **With a board:** a duplicate `command_id` across a **physical** power
      cycle is still deduplicated (the NVS ring survived).
- [ ] **With a board:** an oversized command is clamped or rejected on silicon.
- [ ] **With a board: HIL-1 passes on a multimeter** — the pump line never
      asserts across 20 resets, a watchdog reset, and 10 mid-boot power cuts.
- [ ] **With a board:** withholding `edge.time` refuses every water command with
      `clock_unsynced` while telemetry continues.
- [ ] **With a board:** 20 consecutive deep-sleep wake cycles with no missed
      wake and no watchdog reset (M9-019).
- [ ] **With a board:** both rails measurably off during deep sleep (M9-020).
- [ ] **With a board:** HIL-2.
- [ ] **With a board:** HIL-2's bench checklist.

**HIL-1 is the one that matters.** If the pump line so much as twitches across
those resets, the hardware pull-down is wrong and no firmware correctness
compensates — the window between reset and the first statement of `main` is not
coverable from inside `main`. The requirement is documented in
`src/board/devkitm1.rs` beside the pin it constrains, and every future board
profile carries the same note for its own pin.

---

## 5. The pending-result ledger decision (M9-011)

**Capacity 16. No eviction. Actuation refused at 15, with
`RejectReason::ResultLedgerFull`.**

Recorded against ADR-014's six points in
[ADR-014](../adr/014-failure-and-retry-policy.md) §Device-side pending-result
ledger and in [PRD 090](../prd/090-esp32-rust-firmware.md) Open question 5, both
now resolved. In brief:

1. **New actuation is refused while saturated.** The check is a device-local
   veto that runs **after** `validate_water_command` has already accepted — the
   same shape M16's delivery witness will take — so the shared gate stays the
   only gate and this can only ever stop a dose.
2. **Already-delivered water stays attributable.** Nothing is evicted, and
   `delivered_today_ml` rides on every result and in `device.status` as an
   independent running total.
3. **Saturation is visible** as a durable fault, latched once per episode and
   cleared on the crossing back, reported with the volume the edge has not yet
   counted.
4. **Recovery is per `command_id`**; an unknown acknowledgement is a no-op.
5. **Reboot at the boundary is safe** — the ledger is persisted before the
   publish, into the inactive of two CRC-protected NVS slots.
6. **No eviction is adopted**, and no safety-equivalence argument is offered,
   because there is not one to make.

**Why a new refusal reason rather than reusing one.** `pump_unavailable` was the
cheap option and it would have told the edge something false: the pump is fine.
An operator chasing a pump fault that does not exist is worse served than one
told the ledger is full. `RejectReason::ResultLedgerFull` was added additively
to the shared contract; both ends already decode an unrecognised reason to
`unknown`, so a device or an edge that predates it still interoperates.

**Why the reserved slot.** A refusal is *itself* a `command.result` and needs
somewhere to live. Without the reserve the device could reach a state where it
cannot record the refusal it just issued. If the ledger is nonetheless
completely full, a **rejection** is published once, un-ledgered and unretried —
sound for that one class of result and no other, because a rejection reports
zero delivered water and losing it cannot under-count anything.

**Why 16.** It matches `COMMAND_DEDUP_RING`. A deeper ledger could hold a result
for a command the ring had already forgotten.

---

## 6. Deviations and findings

**Two workspaces, not `src/app/`.** ADR-007's illustrative layout is not what
was built; the isolation it exists for is stronger. ADR-007 permitted this
explicitly and has been updated to describe the layout as built.

**The structural tests live in `node-app/tests/`, not `esp32-node/tests/`.**
M9-003 asks for a structural board-isolation test "run as an ordinary test in
the firmware workspace". `esp32-node` builds for the ESP target, so `cargo test`
there would cross-compile a binary that cannot run without a board. The checks
are on source text and need no target, so they run in the workspace that can
execute them — with no board, no ESP-IDF, and no nightly. They scan
`../esp32-node/src` and assert they found files first, so a moved crate fails
loudly rather than passing vacuously.

**`credit_window` had a real hang.** Written as a subtract-in-a-loop, a credit
of `u64::MAX` — which a corrupted RTC word can present — is about 2×10¹¹
iterations against a day-long window. On an ESP32 that is a watchdog reset
inside the accounting code, which is the last place to hang. Found by a host
test taking 60 seconds; fixed with `%`. This is precisely the class of bug the
host-testable split exists to catch.

**The conformance suite found a wire divergence on its first run** —
`delivered_ml: Some(0.0)` versus `null` on a rejection. See §3.

**The first CI run reached the M8 scenario suite for the first time ever, and
found a latent race in it.** Every previous `End-to-end` run had died in setup
after ~25 s (`gen-dev-env.sh: Permission denied`, fixed by the preceding
commit), so no scenario had ever executed on a runner. This one got there and
`scenario_normal_telemetry` failed with `telemetry counts are inconsistent:
(66, 11, 11)` — eleven batches where the assertion demanded exactly ten.

The scenario read `count(DISTINCT batch_id)`, saw ten, then stopped the
simulator and re-read. Stopping a container is not instantaneous and a batch
already on the broker is still delivered afterwards, so an eleventh batch landed
between the two reads. **The assertion was unprovable as written**: nothing can
stop a publisher at an exact count.

Fixed by asserting the property rather than the number — the batch count must
*reach* ten, and then, on a snapshot taken once ingestion has settled, every
batch must carry exactly one `received_at`, no two batches may share one, every
batch must have stored at least one sample, and `received_at` must never
regress. Batch atomicity is what protocol §3 actually promises, and it is true
of eleven batches exactly as it is of ten. The observation that failed CI —
`(66, 11, 11)` — satisfies every one of the new assertions.

Fixing that exposed a **second** latent flake immediately behind it.
`scenario_rolling_cap_across_midnight` spends the daily cap, waits for the next
virtual midnight, and asserts a further dose is still refused. But the cap is a
**rolling 24-hour window derived from rows** (SAFETY-006), not a calendar day —
so the assertion is only meaningful while the doses are still *inside* that
window, and whether they are depends on where in the virtual day the scenario
happens to start.

The diagnostics settled it with numbers rather than argument. The five doses
were issued from virtual 1788392784012; the sixth, which was accepted, at
1788480316815 — **24.315 virtual hours** after the first. The window had
genuinely elapsed and released 60 ml. **The edge was right and the invariant
held**; the scenario asserted something false. It had always been one virtual
hour from failing, and the settle loop added to `normal_telemetry` shifted the
clock just far enough to tip it.

Fixed by approaching the boundary deliberately: wait until midnight is within
two virtual hours, *then* spend the cap, *then* cross. The doses are a couple of
virtual hours old when the check runs, which is the situation the invariant is
about, and the premise is now asserted explicitly so a future drift reports
"this scenario is testing nothing" instead of passing vacuously.

Both are M8 code, not M9, and are recorded here because M9's commit is what
exposed them. PRD 080 is explicit that a flaky scenario is fixed and never
retried to green.

**The e2e job's budgets had never been measured either, and getting them right
took two attempts.** With both scenarios fixed the suite ran clean and the *job*
was killed at 15 minutes: setup — checkout, buildx, the bake build, image
extraction — took **14m16s**, leaving 57 seconds in which 14 of the 41 scenarios
ran. The budget had been set when the job had never reached a scenario, so it
had never had to fit one.

Raising it to 30 minutes with a 10-minute budget on the suite step was the
right *shape* — a hung suite and a slow build should fail differently — but the
step number was extrapolated from those first 14 scenarios, which are the fast
ones. **The suite is not uniform.** Measured on a runner: median 3 s,
`scenario_long_isolation` 50 s, `scenario_offline_budget` 73 s, and
`scenario_history_gap` several minutes because it has to fill a 256-entry
telemetry buffer at the simulator's cadence. Thirty-three scenarios took 272 s
and the thirty-fourth was still running when the step budget expired.

**The underlying defect was a budget in the wrong unit.** `wait_simulator` takes
an *iteration count*, and each iteration costs an HTTP round trip whose latency
belongs to the machine — so `history_gap`'s 8 000 attempts meant about a minute
on a developer's desktop and about five and a half on a two-core runner sharing
itself with six containers. It sat within seconds of its own limit, which is the
worst place for a limit to be. It now waits against a **wall-clock deadline**
(`wait_simulator_until`, 10 minutes), so the budget means the same thing
everywhere and the failure carries the last observed simulator state.

Final budgets, from measurement rather than extrapolation: the full suite takes
**648 s for all 41 scenarios** on a fast developer machine and a runner is
roughly 1.5–2× slower again, so the suite step is 30 minutes and the job 50.

**A structural test caught a catch-all arm in `credit_elapsed`.** The inner
match had `_ => MonotonicMillis(0)`, which returns the *conservative* value and
was therefore safe — but a catch-all in a safety classifier is how a third case
one day acquires an answer nobody decided on. Rewritten as `Some(_) | None`.

**Protocol §5.10 contained a permission ADR-014 later forbade.** It said a
device "MAY … evict oldest first". That predates the ledger analysis and is not
safe as written. The bound stays permitted; the eviction does not. Corrected,
with the change logged in §9.

**`cargo-generate` is not needed** and has been dropped from the setup
commands: the firmware crate exists and is not generated from a template.

**`safety_007_simulator_refuses_like_hardware` is timing-sensitive under load,
and it is not an M9 regression.** It failed once during this pass with
`reason: "expired"` — the command's 120 s TTL elapsed before the simulator
processed it — while a Docker image build was saturating the machine. It then
passed three consecutive times on a quiet machine, and the failure mode is the
TTL check, which nothing in this pass touches: the only contract change is an
additive `RejectReason` variant. Recorded rather than dismissed, because a
broker-backed test that fails under contention will fail again on a busy CI
runner, and the next person to see it should not spend an afternoon on M9.

**Telemetry publishes nothing yet, by design.** M9 ships fake adapters and no
sensors are fitted, so the sampled batch is empty and nothing is published. The
sampling code, the ring, the envelope, and the error counters are all
host-tested; M10 fits the probe.

**`gate_inputs` in `run.rs` reports `LeakState::Unknown`.** With no leak sensor
fitted the shared gate refuses every dose with `leak_unknown`. That is
fail-closed and correct — a device with no leak sensor cannot water — and it is
the first thing to check when a dose is refused on a bench before M10.

---

## 7. Issue status

| Issue | State |
|---|---|
| M9-001 verify the toolchain | **DONE** — §1; ADR-007 corrected |
| M9-002 firmware CI job | **DONE — observed passing** (run 33690316017): `firmware-app` 1m42s, `firmware-image (board-devkitm1)` 8m50s cold on Linux, both green. Warm-cache timing not yet measured |
| M9-003 workspace and module layout | **DONE** — both workspaces, board layer, structural tests |
| M9-004 NVS and identity | **DONE (host)** — real NVS adapter IMPLEMENTED, HARDWARE PENDING |
| M9-005 hardware traits and fakes | **DONE** — nine traits, fakes for all, every failure mode |
| M9-006 serial provisioning | **PARTIAL** — parser, redaction, unlock and NVS writes host-tested; the console is not wired to the UART. HARDWARE PENDING |
| M9-007 boot-safe pump state | **DONE (host)** — ordering asserted; HIL-1 PENDING |
| M9-008 Wi-Fi | **IMPLEMENTED, HARDWARE PENDING** — backoff is host-testable and is not yet unit-tested |
| M9-009 MQTT, LWT, time sync | **IMPLEMENTED, HARDWARE PENDING** — the `edge.time` rules are shared-crate code and are host-tested there |
| M9-010 telemetry | **PARTIAL** — sampling, ring, envelopes host-tested; publishing HARDWARE PENDING |
| M9-011 command handling and dedup | **DONE (host)** — including the ledger decision |
| M9-012 config handling | **DONE (host)** |
| M9-013 interrupted dose | **DONE (host)** |
| M9-014 conformance | **DONE** — 12 tests |
| M9-015 NVS policy store | **DONE (host)** — interruption at every step |
| M9-016 offline evaluator | **DONE (host)** — one call site |
| M9-017 event buffer | **DONE (host)** |
| M9-018 monotonic budget | **DONE (host)** |
| M9-019 power mode and deep sleep | **IMPLEMENTED, HARDWARE PENDING** — the wake machine and `credit_elapsed` are host-tested; sleep itself is not |
| M9-020 rails and warm-up | **IMPLEMENTED, HARDWARE PENDING** — rail guard and warm-up host-tested |
| M9-021 awake hold and battery telemetry | **PARTIAL** — hold guard and battery-field absence host-tested; the divider needs a board that has one |
| M9-022 M9 verification | **NOT STARTED** — it is the exit gate and needs the board |

---

## 8. What to run when the board arrives

**First command, before anything else is attached:**

```powershell
$env:CARGO_TARGET_DIR = "D:\rzt-fw"
. $HOME\export-esp.ps1
cd firmware/esp32-node
cargo build --release
espflash flash --monitor $env:CARGO_TARGET_DIR\riscv32imc-esp-espidf\release\esp32-node
```

**With a multimeter on the pump driver input and NOTHING ELSE CONNECTED**, and
before a pump is ever wired: confirm the line reads inactive from power-on,
through the bootloader, and into `main`. That is HIL-1's first case and it is
the one measurement that cannot be deferred — everything else in M9 can be
re-run later, but a pump wired to a line that floats active will move water
during the first reset that happens while somebody is holding it.

Then, in order: HIL-1's twenty resets, its watchdog reset, and its ten mid-boot
power cuts; provisioning over serial; association and the retained status; the
duplicate `command_id` across a physical power cycle; the oversized command; the
withheld `edge.time`; and only then the battery-mode wake cycles.

---

## 9. Exit criteria

**M9 is NOT done.** ROADMAP §2's M9 exit criteria include HIL-1 and five other
board-dependent items, none of which has been attempted. The milestone status is
**IN PROGRESS**, and **M10 is NOT READY**.
