[package]
name = "rhizo-docscheck"
description = "Planning-artefact consistency validator for Rhizo Edge"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
publish.workspace = true

# Deliberately dependency-free: this must build and run in CI with no network
# and no registry access, before any other crate compiles. It validates
# planning artefacts and is never shipped or depended on by a runtime crate.
[dependencies]

[[bin]]
name = "rhizo-docscheck"
path = "src/main.rs"

[lints]
workspace = true
